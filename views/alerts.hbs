{{> header}}
{{> nav}}
<div id="page-wrapper">
    <div class="row">
        <div class="col-lg-12">
            <h1 class="page-header">All Alerts</h1>
        </div>
        <!-- /.col-lg-12 -->
    </div>
    <!-- /.row -->
    <div class="row">
        <div class="col-lg-12">
            <table class="product_detail_table table table-striped table-hover">
                <thead>
                    <tr>
                        <th>Title</th>
                        <th>Description</th>
                        <th>Created On</th>
                        <th>View</th>
                        <th>Status</th>
                    </tr>      
                </thead>
                
                <tbody>
                    {{#each alerts as |alert productvkey|}}
                    <tr>
                        <td>{{alert.title}}
                            <a href="/pages/orders/alerts/{{alert._id}}">Mark as Read</a> | 
                            
                        </td>
                         <td>
                            {{!-- {{#each productvalue.deliverydate}}
                                {{this.categorytitle}}
                            {{/each}} --}}
                            {{alert.content}}
                        </td>
                        <td>{{alert.notificationtime}}</td>
                        <td><a href="/pages/orders/view/{{alert.vieworder}}">{{alert.vieworder}}</a></td>
                        <td>{{alert.readflag}}</td>
                    </tr>
                    {{/each}}    
                </tbody>
                {{!-- <tfoot>
                    <tr>
                        <th>S.no</th>
                        <th>Name</th>
                        <th>Category</th>
                        <th>Sku</th>
                        <th>Type</th>
                        <th>Date</th>
                    </tr>
                </tfoot> --}}
            </table>
        </div>
    </div>
</div>
<!-- /#page-wrapper -->
{{> footer}}