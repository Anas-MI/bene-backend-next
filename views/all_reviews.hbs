{{> header}}

{{> nav}}
<script>
$(document).ready(function(){
  $('[data-toggle="tooltip"]').tooltip();   
});
</script>



<div id="page-wrapper">
    <div class="row">
        <div class="col-lg-12">
            <h1 class="page-header">All Reviews</h1>
        </div>
        <!-- /.col-lg-12 -->
    </div>
    <!-- /.row -->
    <div class="row">
        <div class="col-lg-12">
            <table class="product_detail_table table table-striped table-hover table-bordered">
                <thead>
                    <tr>
                        <th>Title</th>
                        <th>Content</th>
                        <th></th>
                    </tr>      
                </thead>  
                <tbody>
                    {{#each reviews as |review|}}
                    <tr>

                        <td><p href="#" data-toggle="tooltip" data-placement="right" data-container="body" title="{{review.title}} || {{review.title}}" data-original-title="">{{review.title}}</p></td>
                        
                        
                         <td>
                             <p href="#" data-toggle="tooltip" data-placement="right"   data-html="true" title="Overall: {{review.overall}} ||
                             Effectiveness: {{review.effectiveness}} ||
                             Quality: {{review.quality}} ||
                             Value for Money: {{review.vmoney}} ||" data-original-title="">{{review.content}}</p>
                            {{!-- <a href="/pages/orders/view/{{ordervalue._id}}">{{ordervalue.userdetails.email}}</a>  --}}
                        </td>
                        <td style="width: 100px;">
                                                    <span class="btn btn-danger creatives_delete" data-id="{{review._id}}">X</span>
                        <span class="btn btn-primary creatives_approve" data-reviewid="{{review._id}}">✔</span>

                            {{!-- {{#equal ordervalue.userid.role 'broker'}}
                                {{orderv.alue.userid.name}}
                            {{else}}
                                {{this.brokername}}
                            {{/equal}} --}}
                        </td>
                        
                    </tr>
                    {{/each}}    
                </tbody>
            </table>
        </div>
    </div>
</div>
<!-- /#page-wrapper -->
<!-- /#page-wrapper -->
{{> footer}}
